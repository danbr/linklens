# LinkLens agent instructions

Before architecture, source-adapter, CLI, schema, or prompt changes, read `docs/adr/`.

Treat ADRs as repo memory:
- follow accepted ADRs unless the task explicitly changes the decision
- update or add an ADR when a durable product/architecture decision changes
- run `npm run adr:check` with tests before shipping repo-level behavior changes
